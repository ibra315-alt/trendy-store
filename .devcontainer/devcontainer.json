{
  "name": "Trendy Store",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "postCreateCommand": "npm install && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed.ts",
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Trendy Store",
      "onAutoForward": "openBrowser",
      "visibility": "public"
    }
  },
  "postStartCommand": "npm run dev"
}
